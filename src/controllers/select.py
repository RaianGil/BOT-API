def sum(in_model, in_sum):
    result = 0
    for bot1 in in_model:
        sum = 0
        try:
            sum = result
        except:
            None
        result = sum + bot1[in_sum]
    return result